#!/usr/bin/env bash
# ============================================================
# Extract the first 20 characters of each line and save to a new file.
# ============================================================

cut -c1-20 input.txt > output.txt
